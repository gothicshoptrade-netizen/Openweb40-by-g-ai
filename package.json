{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npm run build && npm run start",
    "build": "npx pnpm install && npx pnpm run typecheck && npx pnpm -r --if-present run build",
    "start": "npx pnpm --filter @workspace/api-server start",
    "typecheck:libs": "tsc --build",
    "typecheck": "npx pnpm run typecheck:libs && npx pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "motion": "^12.38.0"
  }
}
